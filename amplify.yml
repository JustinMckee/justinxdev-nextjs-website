version: 1
frontend:
  phases:
    preBuild:
      commands:
        - 'npx pnpm install --config.strict-dep-builds=false'
    build:
      commands:
        - 'npx pnpm run build'
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - '.next/cache/**/*'
      - 'node_modules/**/*'
